import {
    NavLink,
    Link
} from 'react-router-dom';

const NavBar = (props: {}) => {
    return (
        <nav className="Header-nav">
            <div className="nav">

            </div>
        </nav>
    );
};

export default NavBar;