const HostList = (props: { form: string }) => {
    return (
        <select form={props.form} multiple className="block px-4 py-3 rounded" required>
            
        </select>
    );
};

export default HostList;